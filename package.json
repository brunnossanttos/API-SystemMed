{
  "name": "api-systemmed",
  "version": "1.0.0",
  "description": "API para estudos de Mongodb e Docker",
  "main": "index.js",
  "scripts": {
    "dev": "set NODE_ENV=dev && set NODE_ENV=dev && ts-node-dev -r tsconfig-paths/register --inspect --transpile-only --ignore-watch node_modules src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bruno Santos",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.24",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
